/**
 * Schema versions for city packages and live manifests.
 * Re-exported from the domain layer so content tooling and domain stay aligned.
 */

export {
  CATALOG_SCHEMA_VERSION,
  CITY_PACKAGE_SCHEMA_VERSION,
  MANIFEST_SCHEMA_VERSION,
} from '../../domain/catalog/schemaVersions.js'
