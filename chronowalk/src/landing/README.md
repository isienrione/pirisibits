# V1 — Cinematic Document (saved snapshot)

Saved **2026-07-09**. Full dark-theater landing: hero seam, obsidian editorial panels, threshold demo, Rome artifact, sticky CTA.

## Restore this version

Copy every file in this folder back to `src/landing/` (overwrite):

```bash
cp src/landing/archive/v1-cinematic-document/* src/landing/
```

Then rebuild: `npm run build`

## Preview

`npm run dev` → http://localhost:5173/landing
