# pirisibits

Side-project monorepo. The main app lives in **[chronowalk/](chronowalk/)** — see [chronowalk/README.md](chronowalk/README.md) for setup, environment variables, and Mapbox configuration.

The Santiago engine reconstruction (Gates 2E.2E.1-R through 2E.3.2-R) is exported under **[santiago-engine/](santiago-engine/)** as a snapshot of `8e62ac67`. It is a separate Expo/TypeScript tree and does not replace the iOS ChronoWalk product.
